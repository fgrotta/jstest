var App = App || {};

App.Assert = (function(App) {
    /*
    * Add this assertions:
    * isTrue
    * isFalse
    * isNull
    * isUndefined
    * areEquals
    * areNotEquals
    * arraysAreEquals
    * arraysAreNotEquals
    * throw an exception if it doesn't pass the test
    * */

}) (App);